# Auto-generated by DjangoCFG - see CLAUDE.md

from __future__ import annotations
import datetime
from typing import Any

from pydantic import BaseModel, ConfigDict, Field

from ..enums import PatchedSkillUpdateRequestStatus, PatchedSkillUpdateRequestVisibility


class SkillCategory(BaseModel):
    """
    Serializer for SkillCategory model.

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    id: str = ...
    name: str = Field(max_length=100)
    slug: Any = Field(pattern='^[-a-zA-Z0-9_]+$')
    description: str | None = None
    icon: str | None = Field(
    None,
    description='Icon identifier (e.g., material ...',
    max_length=50,
)
    ordering: int | None = Field(None, ge=0, le=2147483647)
    skill_count: int = ...



class SkillTag(BaseModel):
    """
    Serializer for SkillTag model.

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    id: str = ...
    name: str = Field(max_length=50)
    slug: Any = Field(pattern='^[-a-zA-Z0-9_]+$')



class SkillList(BaseModel):
    """
    Serializer for skill list view (lightweight).

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    id: str = ...
    slug: Any = Field(
    description='Globally unique slug for this skill',
    pattern='^[-a-zA-Z0-9_]+$',
)
    name: str = Field(max_length=150)
    short_description: str | None = Field(
    None,
    description='Brief summary shown on skill cards',
    max_length=300,
)
    author: int = ...
    author_username: Any = ...
    category: str | None = None
    category_name: Any | None = None
    tags: list[SkillTag] = ...
    visibility: PatchedSkillUpdateRequestVisibility | None = Field(
    None,
    description='* `public` - Public * `private` ...',
)
    status: PatchedSkillUpdateRequestStatus | None = Field(
    None,
    description='* `draft` - Draft * `published` ...',
)
    is_verified: bool = Field(description='Admin-verified trusted skill')
    icon: str | None = Field(None, description='Card thumbnail image')
    cover: str | None = Field(None, description='Detail page banner image')
    install_count: int = Field(description='Denormalized install counter')
    star_count: int = Field(description='Denormalized star counter')
    download_count: int = ...
    review_count: int = ...
    is_starred: bool | None = None
    upstream_author_name: Any = ...
    upstream_author_avatar: str = ...
    upstream_author_url: str = ...
    created_at: datetime.datetime = ...
    updated_at: datetime.datetime = ...



class PaginatedSkillListList(BaseModel):
    """
    
    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    count: int = Field(description='Total number of items across all...')
    page: int = Field(description='Current page number (1-based)')
    pages: int = Field(description='Total number of pages')
    page_size: int = Field(description='Number of items per page')
    has_next: bool = Field(description='Whether there is a next page')
    has_previous: bool = Field(description='Whether there is a previous page')
    next_page: int | None = Field(None, description='Next page number (null if no nex...')
    previous_page: int | None = Field(None, description='Previous page number (null if no...')
    results: list[SkillList] = Field(description='Array of items for current page')



class SkillCreateRequest(BaseModel):
    """
    Serializer for creating a skill.

    Request model (no read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    name: str = Field(min_length=1, max_length=150)
    short_description: str | None = Field(
    None,
    description='Brief summary shown on skill cards',
    max_length=300,
)
    description: str | None = Field(None, description='Full description with markdown s...')
    category: str | None = None
    tags: list[str] | None = Field(None, description='Tag names (strings). Tags are cr...')
    visibility: PatchedSkillUpdateRequestVisibility | None = Field(
    None,
    description='* `public` - Public * `private` ...',
)
    icon: Any | None = None
    cover: Any | None = None
    repository_url: str | None = Field(
    None,
    description='Link to source code repository',
    max_length=200,
)



class SkillCreate(BaseModel):
    """
    Serializer for creating a skill.

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    name: str = Field(max_length=150)
    short_description: str | None = Field(
    None,
    description='Brief summary shown on skill cards',
    max_length=300,
)
    description: str | None = Field(None, description='Full description with markdown s...')
    category: str | None = None
    tags: list[str] | None = Field(None, description='Tag names (strings). Tags are cr...')
    visibility: PatchedSkillUpdateRequestVisibility | None = Field(
    None,
    description='* `public` - Public * `private` ...',
)
    icon: str | None = None
    cover: str | None = None
    repository_url: str | None = Field(
    None,
    description='Link to source code repository',
    max_length=200,
)



class SkillReview(BaseModel):
    """
    Serializer for SkillReview in API responses.

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    id: str = ...
    author_handle: Any = ...
    author_display_name: Any = ...
    author_avatar_url: str = ...
    body: Any = ...
    is_rewritten: bool = ...
    source: Any = ...
    upstream_created_at: datetime.datetime | None = None



class SkillFile(BaseModel):
    """
    Serializer for a single file in a skill version.

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    path: str = Field(description='Relative file path, e.g. "skill....')
    content: str = Field(description='UTF-8 text content')



class SkillVersion(BaseModel):
    """
    Serializer for SkillVersion model.

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    id: str = ...
    skill: str = ...
    version: str = Field(description='Semantic version string (e.g., 1...', max_length=20)
    files: list[SkillFile] = ...
    changelog: str | None = Field(None, description='What changed in this version')
    source_archive: str | None = None
    is_latest: bool = Field(description='Denormalized flag: only one vers...')
    created_at: datetime.datetime = ...



class SkillDetail(BaseModel):
    """
    Serializer for skill detail view (full data).

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    id: str = ...
    slug: Any = Field(
    description='Globally unique slug for this skill',
    pattern='^[-a-zA-Z0-9_]+$',
)
    name: str = Field(max_length=150)
    short_description: str | None = Field(
    None,
    description='Brief summary shown on skill cards',
    max_length=300,
)
    description: str | None = Field(None, description='Full description with markdown s...')
    author: int = ...
    author_username: Any = ...
    category: SkillCategory = ...
    tags: list[SkillTag] = ...
    visibility: PatchedSkillUpdateRequestVisibility | None = Field(
    None,
    description='* `public` - Public * `private` ...',
)
    status: PatchedSkillUpdateRequestStatus | None = Field(
    None,
    description='* `draft` - Draft * `published` ...',
)
    is_verified: bool = Field(description='Admin-verified trusted skill')
    icon: str | None = Field(None, description='Card thumbnail image')
    cover: str | None = Field(None, description='Detail page banner image')
    install_count: int = Field(description='Denormalized install counter')
    star_count: int = Field(description='Denormalized star counter')
    download_count: int = ...
    repository_url: str | None = Field(
    None,
    description='Link to source code repository',
    max_length=200,
)
    latest_version: SkillVersion | None = None
    is_starred: bool | None = None
    reviews: list[SkillReview] = ...
    review_count: int = ...
    upstream_author_name: Any = ...
    upstream_author_avatar: str = ...
    upstream_author_url: str = ...
    upstream_github_url: Any = ...
    upstream_npm_package: Any = ...
    upstream_license: Any = ...
    created_at: datetime.datetime = ...
    updated_at: datetime.datetime = ...



class SkillUpdateRequest(BaseModel):
    """
    Serializer for updating a skill.

    Request model (no read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    name: str = Field(min_length=1, max_length=150)
    short_description: str | None = Field(
    None,
    description='Brief summary shown on skill cards',
    max_length=300,
)
    description: str | None = Field(None, description='Full description with markdown s...')
    category: str | None = None
    tags: list[str] | None = Field(None, description='Tag names (strings). Tags are cr...')
    visibility: PatchedSkillUpdateRequestVisibility | None = Field(
    None,
    description='* `public` - Public * `private` ...',
)
    status: PatchedSkillUpdateRequestStatus | None = Field(
    None,
    description='* `draft` - Draft * `published` ...',
)
    icon: Any | None = None
    cover: Any | None = None
    repository_url: str | None = Field(
    None,
    description='Link to source code repository',
    max_length=200,
)



class SkillUpdate(BaseModel):
    """
    Serializer for updating a skill.

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    name: str = Field(max_length=150)
    short_description: str | None = Field(
    None,
    description='Brief summary shown on skill cards',
    max_length=300,
)
    description: str | None = Field(None, description='Full description with markdown s...')
    category: str | None = None
    tags: list[str] | None = Field(None, description='Tag names (strings). Tags are cr...')
    visibility: PatchedSkillUpdateRequestVisibility | None = Field(
    None,
    description='* `public` - Public * `private` ...',
)
    status: PatchedSkillUpdateRequestStatus | None = Field(
    None,
    description='* `draft` - Draft * `published` ...',
)
    icon: str | None = None
    cover: str | None = None
    repository_url: str | None = Field(
    None,
    description='Link to source code repository',
    max_length=200,
)



class PatchedSkillUpdateRequest(BaseModel):
    """
    Serializer for updating a skill.

    Request model (no read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    name: str | None = Field(None, min_length=1, max_length=150)
    short_description: str | None = Field(
    None,
    description='Brief summary shown on skill cards',
    max_length=300,
)
    description: str | None = Field(None, description='Full description with markdown s...')
    category: str | None = None
    tags: list[str] | None = Field(None, description='Tag names (strings). Tags are cr...')
    visibility: PatchedSkillUpdateRequestVisibility | None = Field(
    None,
    description='* `public` - Public * `private` ...',
)
    status: PatchedSkillUpdateRequestStatus | None = Field(
    None,
    description='* `draft` - Draft * `published` ...',
)
    icon: Any | None = None
    cover: Any | None = None
    repository_url: str | None = Field(
    None,
    description='Link to source code repository',
    max_length=200,
)



class SkillPackages(BaseModel):
    """
    Package dependencies for a skill.

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    pip: list[str] | None = Field(None, description='pip packages to install, e.g. ["...')
    npm: list[str] | None = Field(None, description='npm packages to install, e.g. ["...')



class SkillInstall(BaseModel):
    """
    Response serializer for install action.

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    slug: str = Field(description='Skill slug')
    version: str = Field(description='Version installed')
    install_count: int = Field(description='Updated install count')
    files: list[SkillFile] = Field(description='All files that make up this skill')
    packages: SkillPackages = ...



class PaginatedSkillReviewList(BaseModel):
    """
    
    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    count: int = Field(description='Total number of items across all...')
    page: int = Field(description='Current page number (1-based)')
    pages: int = Field(description='Total number of pages')
    page_size: int = Field(description='Number of items per page')
    has_next: bool = Field(description='Whether there is a next page')
    has_previous: bool = Field(description='Whether there is a previous page')
    next_page: int | None = Field(None, description='Next page number (null if no nex...')
    previous_page: int | None = Field(None, description='Previous page number (null if no...')
    results: list[SkillReview] = Field(description='Array of items for current page')



class SkillStar(BaseModel):
    """
    Response serializer for star toggle action.

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    starred: bool = Field(description='Whether the skill is now starred')
    star_count: int = Field(description='Updated star count')



class SkillListRequest(BaseModel):
    """
    Serializer for skill list view (lightweight).

    Request model (no read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    name: str = Field(min_length=1, max_length=150)
    short_description: str | None = Field(
    None,
    description='Brief summary shown on skill cards',
    max_length=300,
)
    category: str | None = None
    visibility: PatchedSkillUpdateRequestVisibility | None = Field(
    None,
    description='* `public` - Public * `private` ...',
)
    status: PatchedSkillUpdateRequestStatus | None = Field(
    None,
    description='* `draft` - Draft * `published` ...',
)
    icon: Any | None = Field(None, description='Card thumbnail image')
    cover: Any | None = Field(None, description='Detail page banner image')



class SkillUploadCoverRequestRequest(BaseModel):
    """
    Request serializer for cover image upload.

    Request model (no read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    cover: Any = Field(description='Cover image file')



class SkillUploadCoverResponse(BaseModel):
    """
    Response serializer for cover image upload.

    Response model (includes read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    cover: str = Field(description='URL of the uploaded cover image')



class SkillFileRequest(BaseModel):
    """
    Serializer for a single file in a skill version.

    Request model (no read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    path: str = Field(description='Relative file path, e.g. "skill....', min_length=1)
    content: str = Field(description='UTF-8 text content', min_length=1)



class SkillVersionCreateRequest(BaseModel):
    """
    Serializer for creating a new skill version.

    Request model (no read-only fields).
    """

    model_config = ConfigDict(
        validate_assignment=True,
        extra="allow",
        frozen=False,
    )

    version: str = Field(description='Semantic version (e.g., 1.0.0)', min_length=1, max_length=20)
    files: list[SkillFileRequest] = Field(description='List of files that make up the s...')
    changelog: str | None = Field(None, description='What changed', min_length=1)



